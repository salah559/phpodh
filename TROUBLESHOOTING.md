# دليل حل مشكلة Internal Server Error 500 في cPanel

## 🔍 تشخيص المشكلة

### الخطوة 1: استخدام ملف الفحص

1. ارفع ملف `check_server.php` إلى cPanel
2. افتحه في المتصفح: `https://yourdomain.com/check_server.php`
3. راجع النتائج لمعرفة المشكلة بالضبط

---

## ✅ الحلول حسب المشكلة

### 🔴 المشكلة 1: امتداد `pdo_pgsql` غير مُفعّل

**هذه هي المشكلة الأكثر شيوعاً!**

#### الحل أ: تفعيل PostgreSQL في cPanel

**للأسف، معظم استضافات cPanel لا تدعم PostgreSQL بشكل جيد.** لديك 3 خيارات:

#### **الخيار 1: التواصل مع الدعم الفني** ⭐ (الأسهل)

اطلب من الدعم الفني تفعيل:
- امتداد `pdo_pgsql`
- PostgreSQL Database Support

#### **الخيار 2: تحويل المشروع لاستخدام MySQL** (بديل موصى به)

معظم استضافات cPanel تدعم MySQL. يمكنني مساعدتك في تحويل المشروع:

```php
// سيتم تغيير ملفات قاعدة البيانات لاستخدام MySQL بدلاً من PostgreSQL
```

#### **الخيار 3: استخدام استضافة أخرى**

استخدم استضافة تدعم PostgreSQL مثل:
- **Heroku** (مجاني للمشاريع الصغيرة)
- **DigitalOcean**
- **Railway.app**
- **Render.com**
- **Replit** (حيث نحن الآن!)

---

### 🔴 المشكلة 2: خطأ في الاتصال بقاعدة البيانات

#### الحل:

1. **تحقق من معلومات قاعدة البيانات في `config/database.php`:**

```php
define('DB_HOST', '127.0.0.1');  // أو localhost
define('DB_USER', 'your_username');  // من cPanel
define('DB_PASS', 'your_password');  // من cPanel
define('DB_NAME', 'your_database');  // من cPanel
define('DB_PORT', '5432');
```

2. **تأكد من إنشاء قاعدة البيانات في cPanel:**
   - اذهب إلى **PostgreSQL Databases**
   - أنشئ قاعدة بيانات جديدة
   - أنشئ مستخدم وامنحه صلاحيات كاملة

---

### 🔴 المشكلة 3: ملف .htaccess يسبب مشاكل

#### الحل:

1. **احذف ملف `.htaccess` مؤقتاً** أو أعد تسميته لـ `.htaccess.bak`
2. جرب فتح الموقع مرة أخرى
3. إذا نجح، استخدم هذا الإصدار البسيط:

```apache
# ملف .htaccess بسيط
php_flag display_errors off
php_flag log_errors on

Options -Indexes

<FilesMatch "^(config/database\.php)$">
    Order allow,deny
    Deny from all
</FilesMatch>
```

---

### 🔴 المشكلة 4: إصدار PHP قديم

#### الحل:

في cPanel:
1. اذهب إلى **Select PHP Version** أو **MultiPHP Manager**
2. اختر PHP 7.4 أو أحدث (يُفضل 8.0+)
3. احفظ التغييرات

---

### 🔴 المشكلة 5: صلاحيات الملفات

#### الحل:

عبر File Manager في cPanel أو SSH:

```bash
# ضبط صلاحيات الملفات
find . -type f -exec chmod 644 {} \;
find . -type d -exec chmod 755 {} \;

# صلاحيات خاصة لمجلد config
chmod 755 config/
chmod 644 config/*.php
```

---

## 📋 فحص سجلات الأخطاء (Error Logs)

### في cPanel:

1. اذهب إلى **Error Log** أو **Errors**
2. ابحث عن آخر أخطاء حدثت
3. الخطأ سيخبرك بالمشكلة بالضبط

### الأخطاء الشائعة ومعانيها:

| الخطأ | المعنى | الحل |
|------|--------|------|
| `Call to undefined function pg_connect()` | PostgreSQL غير مُفعّل | فعّل pdo_pgsql |
| `could not connect to server` | خطأ في معلومات الاتصال | راجع config/database.php |
| `Parse error` | خطأ في كود PHP | راجع السطر المذكور في الخطأ |
| `.htaccess` في رسالة الخطأ | مشكلة في ملف .htaccess | احذفه مؤقتاً |

---

## 🆘 الحل السريع: تحويل المشروع لـ MySQL

إذا كانت استضافتك لا تدعم PostgreSQL، يمكنني مساعدتك في تحويل المشروع لاستخدام MySQL (الذي تدعمه جميع استضافات cPanel).

### المميزات:
- ✅ متوافق مع جميع استضافات cPanel
- ✅ سهل الإعداد
- ✅ نفس الوظائف بالضبط

### ما يحتاج للتغيير:
- ملف `config/database.php`
- ملف `config/init_db.php` (أنواع البيانات)
- بعض استعلامات SQL البسيطة

**هل تريد المساعدة في التحويل لـ MySQL؟**

---

## ✨ نصائح إضافية

### 1. تفعيل عرض الأخطاء للتطوير فقط

في بداية `index.php` مؤقتاً للتشخيص:

```php
<?php
// للتطوير فقط - احذف بعد حل المشكلة
error_reporting(E_ALL);
ini_set('display_errors', 1);
?>
```

⚠️ **احذف هذا الكود بعد حل المشكلة!**

### 2. فحص امتدادات PHP المطلوبة

تأكد من تفعيل:
- ✅ PDO
- ✅ pdo_pgsql (للـ PostgreSQL) أو pdo_mysql (للـ MySQL)
- ✅ mbstring
- ✅ json
- ✅ curl
- ✅ session

### 3. حفظ نسخة احتياطية

قبل أي تعديل، احفظ نسخة من:
- قاعدة البيانات
- جميع الملفات

---

## 📞 الدعم

إذا جربت كل الحلول ولم تنجح:

1. **أرسل لي سجل الأخطاء** (Error Log) من cPanel
2. **أرسل نتيجة ملف `check_server.php`**
3. سأساعدك في إيجاد الحل المناسب

---

**ملاحظة مهمة:** 
معظم مشاكل 500 Internal Server Error في cPanel تكون بسبب عدم دعم PostgreSQL. 
الحل الأسهل والأسرع هو **تحويل المشروع لاستخدام MySQL** أو **استخدام استضافة تدعم PostgreSQL**.
